# configuración

```js
qacService.config(opts, production);
```

## variables para `opts` y local/global-config.yaml:

parametro                 | uso
--------------------------|--------------------
server.port               | puerto del servidor
services.respository.path | ruta al repositorio
production                | des/activar modo desarrollo
